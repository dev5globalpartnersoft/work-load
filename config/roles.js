/*
It is not recommended to change key names. And the values are possible.
 */

export const ADMIN_ROLE = 'admin';
export const USER_ROLE = 'user';

export const roles = {
  admin: ADMIN_ROLE,
  user: USER_ROLE,
};
