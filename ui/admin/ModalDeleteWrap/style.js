import styled from 'styled-components';

import { Modal } from 'ui/Modal';
import { ActionFormWrap } from '../ActionFormWrap';

export const StyledModal = styled(Modal)``;

export const FormWrap = styled(ActionFormWrap)``;
